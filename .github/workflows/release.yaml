name: Release

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  deployments: write

jobs:
  tests:
     uses: dhruv37502005/Library/.github/workflows/test.yaml@main
     secrets: inherit
  deploy-cf:
    needs: [tests]
    uses: dhruv37502005/Library/.github/workflows/cf.yaml@main
    secrets: inherit
    with:
      environment: Production
